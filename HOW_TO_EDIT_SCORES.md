# 📝 คู่มือการแก้ไขเกณฑ์ให้คะแนน (Scoring & Rules Guide)

คู่มือนี้จะอธิบายวิธีการแก้ไข "เกณฑ์การให้คะแนน" และ "น้ำหนักของคะแนน" ทั้งหมดในระบบ ไม่ว่าจะเป็นเรื่องของปัจจัยพื้นฐาน (Fundamental), เทคนิคอล (Technical), การวิเคราะห์มูลค่า (Valuation) หรือ Master Signal โดยคุณสามารถเปิดไฟล์เหล่านี้ขึ้นมาแก้ไขได้เองด้วยโปรแกรมเช่น **VS Code** หรือ Notepad ครับ

ไฟล์หลักที่คุณจะต้องแก้ไขมีอยู่ 2 ไฟล์ (อยู่ในโฟลเดอร์ `src/utils/`):
1. **`scoringEngine.js`** 👉 สำหรับแก้ไขเกณฑ์ Fundamental และ Valuation 
2. **`technicalEngine.js`** 👉 สำหรับแก้ไขเกณฑ์ Technical, Execution และ Master Signal

---

## 1. การแก้ไขคะแนน Fundamental (พื้นฐาน) 📊
**📂 ไฟล์ที่ต้องแก้:** `src/utils/scoringEngine.js`

ในไฟล์นี้ ให้คุณค้นหาฟังก์ชันที่ชื่อว่า `evaluateStock(payload)`
ด้านในจะมีการแบ่งเกณฑ์ให้คะแนนตาม "ประเภทพอร์ต" (CORE, DEFENSIVE, SATELLITES, ALPHA) 

**🔍 ตัวอย่างวิธีแก้:**
สมมติว่าคุณต้องการแก้เกณฑ์ในพอร์ต **CORE** ว่า *Gross Margin ต้องมากกว่า 55% ถึงจะได้ 10 คะแนน (จากเดิม 50% ได้ 8 คะแนน)*

คุณก็เลื่อนหาบรรทัดนี้ในโค้ด (ในบล็อกของ `if (bucket === 'CORE')`):
```javascript
// โค้ดเดิม
profPass.grossMargin = metrics.grossMargin >= 50;
if (profPass.grossMargin) profScore += 8;
```
แล้วแก้ตัวเลขเป็นแบบนี้:
```javascript
// โค้ดที่แก้แล้ว
profPass.grossMargin = metrics.grossMargin >= 55;
if (profPass.grossMargin) profScore += 10;
```
*💡 ข้อควรระวัง: รวมคะแนนทั้งหมดของแต่ละพอร์ตควรตั้งให้พอดี 100 คะแนนเต็มเพื่อให้แสดงผลในหน้าเว็บได้สมบูรณ์ครับ*

---

## 2. การแก้ไขคะแนน Technical & Timing 📈
**📂 ไฟล์ที่ต้องแก้:** `src/utils/technicalEngine.js`

ในไฟล์นี้ให้หาฟังก์ชัน `calculateTechnicalScore(technicals)`
เกณฑ์จะแบ่งตามคำถามในฟอร์มหน้าเว็บ (เช่น Moving Average, MACD, RSI, Trend)

**🔍 ตัวอย่างวิธีแก้:**
สมมติว่าคุณอยากเพิ่มคะแนนให้ "Strong Uptrend (MA20 > 50 > 200)" จากเดิมได้ 20 คะแนน ให้เป็น 25 คะแนน
หาโค้ดส่วนนี้:
```javascript
// โค้ดเดิม
if (technicals.maTrend === 'STRONG_UP') {
  score += 20;
}
```
แล้วแก้ไขตัวเลข:
```javascript
// โค้ดที่แก้แล้ว
if (technicals.maTrend === 'STRONG_UP') {
  score += 25;
}
```

---

## 3. การแก้ไขเงื่อนไข Master Signal (10 คะแนนเต็ม) ⚡
**📂 ไฟล์ที่ต้องแก้:** `src/utils/technicalEngine.js`

เลื่อนลงมาด้านล่างสุดของไฟล์ จะเจอฟังก์ชัน `calculateMasterSignal({...})`
ที่นี่จะเป็นตัว **"รวมมิตร"** นำคะแนน 4 ด้านมาคำนวณและแปลงสภาพออกมาเป็นคะแนน 10 แต้ม และคำแนะนำ Action

### 3.1 การแก้น้ำหนัก % ของแต่ละพอร์ต
หากคุณต้องการแก้ว่าพอร์ต ALPHA ให้ความสำคัญกับเทคนิคมากกว่าพื้นฐาน คุณสามารถปรับตัวคูณได้:
```javascript
// ตัวอย่างเดิมของ ALPHA
} else if (type === 'ALPHA') {
  fundWeight = 0.15; // พื้นฐาน 15%
  dcfWeight = 0.05;  // มูลค่า 5%
  techWeight = 0.50; // เทคนิคอล 50%
  execWeight = 0.30; // ความคุ้มค่า 30%
}
```
*(ให้แน่ใจว่าตัวคูณทุกตัวบวกกันแล้วได้เท่ากับ 1.0)*

### 3.2 การแก้คำแนะนำและตัวอักษร (Action Description)
เลื่อนลงมาล่างๆ ในฟังก์ชันเดียวกัน จะเจอจุดที่เช็คว่าคะแนนถึงเท่าไหร่ถึงจะให้แนะนำว่า BUY, DCA หรือ HOLD 
คุณสามารถเปลี่ยนเกณฑ์คะแนน หรือเปลี่ยนคำอธิบายในวงเล็บได้เลยครับ:

```javascript
// ตัวอย่างเดิมของ CORE
if (type === 'CORE' || type === 'DEFENSIVE') {
  if (totalScore >= 8.5) {
    actionSignal = 'BUY';
    actionDescription = 'จุดเข้าซื้อคุณภาพสูงที่ไม่ควรพลาด (ของดีราคาถูก)';
  } else if (totalScore >= 6.5) { ... }
```
เปลี่ยนเกณฑ์คะแนน `8.5` หรือแก้คำอธิบายภาษาไทยในเครื่องหมายคำพูด `' '` ได้ตามต้องการเลยครับ

---

## 🚀 หลังจากแก้ไขเสร็จแล้วต้องทำอย่างไร?
1. **กด Save** ไฟล์ในโปรแกรมที่คุณใช้ (เช่น VS Code กด Ctrl+S)
2. แค่นี้เลยครับ! ระบบเว็บที่คุณรันไว้ (Vite) จะทำการอัปเดตโค้ดอัตโนมัติ (Hot-Reload) ภายในพริบตา
3. ลองกลับไปที่หน้าเว็บ แล้วกดปุ่ม "Calculate & Save Signal" ใหม่ เพื่อดูผลลัพธ์จากการเปลี่ยนเกณฑ์ของคุณได้ทันทีครับ!
