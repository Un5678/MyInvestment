# คู่มือการอัปเดตเว็บไซต์ (Cheat Sheet) 🚀

ทุกครั้งที่คุณมีการแก้ไขโค้ด (ไม่ว่าจะแก้เอง หรือให้ AI แก้ให้) เมื่อตรวจสอบผลลัพธ์บนเครื่องตัวเองจนพอใจแล้ว ให้ทำตาม 4 ขั้นตอนนี้ใน Terminal ของ VS Code เสมอครับ:

---

## 1. อัปเดตหน้าเว็บจริง (ให้คนอื่นเห็น)
ก๊อปปี้คำสั่งนี้ไปวางแล้วกด Enter เพื่อให้ระบบแปลงโค้ดและอัปโหลดขึ้นหน้าเว็บทันที:
```bash
npm run deploy
```

---

## 2. เก็บสำรองโค้ด (Backup)
เพื่อป้องกันโค้ดหายและเป็นการบันทึกประวัติการแก้ไข ให้รัน 3 คำสั่งด้านล่างนี้ **เรียงทีละบรรทัด**:

**(2.1 เตรียมไฟล์ที่แก้ไขทั้งหมด)**
```bash
git add .
```

**(2.2 แปะป้ายบอกว่าแก้จุดไหนไปบ้าง - สามารถเปลี่ยนคำว่า update เป็นข้อความอื่นได้)**
```bash
git commit -m "update"
```

**(2.3 ส่งข้อมูลสำรองทั้งหมดขึ้นไปเก็บบน GitHub)**
```bash
git push origin master:main
```

---

> 💡 **ข้อแนะนำเพิ่มเติม:** 
> - ถ้าคุณต้องการรันดูโค้ดบนเครื่องตัวเองก่อนอัปโหลด ให้ใช้คำสั่ง `npm run dev` 
> - ไฟล์นี้ (HOW_TO_UPDATE.md) จะถูกเก็บไว้ในโปรเจกต์ของคุณตลอดไป สามารถเปิดขึ้นมาดูได้เสมอครับ!
